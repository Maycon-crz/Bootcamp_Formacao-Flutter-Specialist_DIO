import 'package:flutter/material.dart';

class HistoricImcPage extends StatelessWidget {
  const HistoricImcPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Histórico de IMC'),
      ),
      body: Container(),
    );
  }
}
